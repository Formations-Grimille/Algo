ALGO CalculFacture

VARIABLES
  txTVA, prixHT, prixTTC : flottant
DÉBUT

  ÉCRIRE("Saisir le prix HT unitaire d'un article")
  LIRE(prixHT)

  ÉCRIRE("Saisir le taux de TVA (20, 10, 5.5 par exemple)")
  LIRE(txTVA)

  prixTTC <- prixHT * (1 + txTVA/100)

  ÉCRIRE("Le prix TTC total est de :", prixTTC, "€")
FIN